repos:
  - repo: local
    hooks:
      - id: shellcheck
        name: shellcheck (all tracked *.sh)
        entry: shellcheck
        language: system
        files: \.sh$
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
